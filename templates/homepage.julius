$(".del").click(function(){
      var name = $(this).parent().find("a1").text()
      url = "@{DeleteR ""}".slice(0, -1) + name
      console.log(url);
      $.post(url);
      window.location.replace("@{HomeR}");
    });
